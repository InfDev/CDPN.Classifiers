@page "/AtdLevels"

@using CDPN.Classifiers.Client.Models;

<h4>Уровни АТУ</h4>

<Atd />

<div class="table scroll mt-2">
    <Table TItem="AtdLevel" IsPagination="false" PageItemsSource="@PageItemsSource" RenderMode="TableRenderMode.Table"
           IsStriped="true" IsBordered="true" UseInjectDataService="true" OnQueryAsync="OnQueryAsync" >
        <TableColumns>
            <TableColumn @bind-Field="@context.Id" Text="Id" Sortable=true Filterable=true ></TableColumn>
            <TableColumn @bind-Field="@context.Name" Text="Наименование" Sortable=true Filterable=true ></TableColumn>
            <TableColumn @bind-Field="@context.InUnitIdStartIndex" Text="Нач. индекс в Id" ></TableColumn>
            <TableColumn @bind-Field="@context.InUnitIdEndIndex" Text="Кон. индекс в Id" ></TableColumn>
        </TableColumns>
    </Table>
</div>
